# Screenshots

The site references these files; each `<img>` hides itself until its file exists,
so add them here and the images appear on the next Pages deploy — no HTML changes needed.

| File | Shown on | What to capture |
|------|----------|-----------------|
| `hero-table.png` | Landing hero, Guide → Table view | Firebase console + side panel, collection loaded in table view with the "Loaded N / M" counter |
| `aggregation.png` | Guide → Aggregation | Aggregation tab, 1–2 fields ticked, bar charts visible |
| `analytics.png` | Guide → Analytics | Analytics tab, metric cards + Records per Day chart |
| `export.png` | Guide → Export | Export controls with the format selector open (JSON / CSV / Excel) |
| `rules-helper.png` | Guide → Rules helper | Guidance panel with generated snippet, copy + retry buttons |

Capture at 1280×800, 100% zoom, light theme, demo data only (never real user data).
The same captures can be annotated/reused for the Chrome Web Store listing (1280×800 PNG, max 5).
